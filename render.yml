services:
  - type: cron
    name: scraper-sika-job
    env: python
    schedule: "*/15 8-15 * * *"
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python scrape_and_insert.py"
    envVars:
      - key: MONGO_URI
        value: ${MONGO_URI}
